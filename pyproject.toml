[project]
name = "mlb-tracker"
version = "0.1.0"
description = ""
authors = [
    {name = "rob",email = "rcgr.13@gmail.com"}
]
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "requests (>=2.34.2,<3.0.0)",
]

[tool.poetry]
packages = [{include = "mlb_tracker", from = "src"}]

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"

[dependency-groups]
dev = [
    "ipykernel (>=7.3.0,<8.0.0)"
]
